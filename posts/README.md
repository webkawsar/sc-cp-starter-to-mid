# C++ এ `while(i--)` কনসেপ্ট বোঝা

এই ছোট প্রোগ্রামটি C++ এর একটি **খুব গুরুত্বপূর্ণ ও কনফিউসিং কনসেপ্ট** বোঝাতে সাহায্য করে —  
`while` loop এর ভিতরে **post-decrement (`i--`) operator** কীভাবে কাজ করে।

---

## 📌 Code Example

```cpp
#include <iostream>
using namespace std;

int main() {
    int i = 5;
    while (i--) {
        cout << i << "\n";
    }
    return 0;
}
```

---

## 🤔 প্রথম দেখায় যে প্রশ্নগুলো আসে

অনেকের মাথায় স্বাভাবিকভাবেই এই প্রশ্নগুলো আসে:

- এটা কি infinite loop?
- `i--` মানে ঠিক কী?
- Output কী হবে?

চল ধাপে ধাপে বুঝি 👇

---

## 🔍 কীভাবে কাজ করছে

এখানে `i--` হলো **post-decrement operator**।  
এর মানে হলো:

1. আগে `i` এর বর্তমান মান দিয়ে condition check হয়
2. তারপর `i` এর মান ১ কমে

### Step-by-step Execution

| Condition Check | Print হয় | নতুন `i` |
|-----------------|----------|----------|
| `i = 5`         | `4`      | `4`      |
| `i = 4`         | `3`      | `3`      |
| `i = 3`         | `2`      | `2`      |
| `i = 2`         | `1`      | `1`      |
| `i = 1`         | `0`      | `0`      |
| `i = 0`         | Loop বন্ধ | —        |

---

## ✅ Output

```
4
3
2
1
0
```

---

## ❗ Common Confusion

অনেকে মনে করেন এই loop টি infinite হবে, কিন্তু আসলে তা নয়।  
যখন `i` এর মান `0` হয়, তখন condition false হয়ে যায় এবং loop শেষ হয়।

---

## 🧠 মূল কথা (Takeaway)

- `i--` এবং `--i` এক জিনিস নয়
- ছোট syntax পার্থক্য বড় logical difference তৈরি করতে পারে
- Debugging এবং interview — দুই জায়গাতেই এই কনসেপ্ট খুব কাজে আসে

---

Happy Coding 🚀  
ভুল বুঝলেই শেখা শুরু হয় 😄
